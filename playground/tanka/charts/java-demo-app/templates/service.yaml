apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-demo-app.fullname" . }}
  labels:
    {{- include "java-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: srvhttp
      protocol: TCP
      name: http
  selector:
    {{- include "java-demo-app.selectorLabels" . | nindent 4 }}
