demoapp:
  common:
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 100m
        memory: 128Mi
agent:
  agent:
    config:
      auto_scale:
        kubernetes:
          enabled: true
