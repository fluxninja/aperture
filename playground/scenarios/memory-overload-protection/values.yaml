demoapp:
  common:
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 100m
        memory: 128Mi
istio:
  envoyfilter:
    envoyFilter:
      authzGrpcTimeout: 2s
agent:
  agent:
    config:
      auto_scale:
        kubernetes:
          enabled: true
