# yaml-language-server: $schema=../../../../blueprints/policies/leaky-bucket-rate-limiting/gen/definitions.json
# Generated values file for policies/leaky-bucket-rate-limiting blueprint
# Documentation/Reference for objects and parameters can be found at:
# https://docs.fluxninja.com/reference/policies/bundled-blueprints/policies/leaky-bucket-rate-limiting
policy:
  policy_name: "leaky-bucket-rate-limiting"
  rate_limiter:
    bucket_capacity: 40
    leak_amount: 2
    leak_interval_ms: 1000
    selectors:
      - service: "service1-demo-app.demoapp.svc.cluster.local"
        control_point: "ingress"
    parameters:
      label_key: "http.request.header.user_id"
