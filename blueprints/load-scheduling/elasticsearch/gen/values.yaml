# Generated values file for load-scheduling/elasticsearch blueprint
# Documentation/Reference for objects and parameters can be found at:
# https://docs.fluxninja.com/reference/blueprints/load-scheduling/elasticsearch

policy:
  # Name of the policy.
  # Type: string
  # Required: True
  policy_name: __REQUIRED_FIELD__
  service_protection_core:
    # Parameters for Range Throttling Strategy.
    # Type: aperture.spec.v1.RangeDrivenLoadSchedulerParameters
    # Required: True
    range_driven_load_scheduler:
      alerter:
        alert_name: "Range Driven Load Throttling Event"
      degree: __REQUIRED_FIELD__
      high_throttle_threshold: __REQUIRED_FIELD__
      load_scheduler:
        selectors:
          - control_point: __REQUIRED_FIELD__
            service: __REQUIRED_FIELD__
      low_throttle_threshold: __REQUIRED_FIELD__
  # Configuration for Elasticsearch OpenTelemetry receiver. Refer https://docs.fluxninja.com/integrations/metrics/elasticsearch for more information.
  # Type: elasticsearch
  # Required: True
  elasticsearch:
    agent_group: "default"
    endpoint: __REQUIRED_FIELD__
    password: __REQUIRED_FIELD__
    username: __REQUIRED_FIELD__
