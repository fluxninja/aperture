{
  /**
  * @param (load_regulator: aperture.spec.v1.LoadRegulatorDynamicConfig required) Default configuration for load regulator that can be updated at the runtime without shutting down the policy.
  */
  load_regulator: '__REQUIRED_FIELD__',
}
