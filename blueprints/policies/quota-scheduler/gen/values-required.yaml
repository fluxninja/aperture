# Generated values file for policies/quota-scheduler blueprint
# Documentation/Reference for objects and parameters can be found at:
# https://docs.fluxninja.com/reference/policies/bundled-blueprints/policies/quota-scheduler

policy:
  # Name of the policy.
  # Type: string
  # Required: True
  policy_name: __REQUIRED_FIELD__
  # List of classification rules.
  # Type: []aperture.spec.v1.Classifier
  classifiers: []
  quota_scheduler:
    # Bucket capacity.
    # Type: float64
    # Required: True
    bucket_capacity: __REQUIRED_FIELD__
    # Fill amount.
    # Type: float64
    # Required: True
    fill_amount: __REQUIRED_FIELD__
    # Flow selectors to match requests against
    # Type: []aperture.spec.v1.Selector
    selectors:
      - control_point: __REQUIRED_FIELD__
        service: __REQUIRED_FIELD__
    # Parameters.
    # Type: aperture.spec.v1.RateLimiterParameters
    parameters:
      # Flow label key to use for quota limiting. If no label key is specified, then all requests matching the selectors will be scheduled based on the global quota.
      # Type: string
      label_key: ""
      # Fill interval e.g. "1s".
      # Type: string
      # Required: True
      interval: __REQUIRED_FIELD__
    # Scheduler configuration.
    # Type: aperture.spec.v1.Scheduler
    scheduler:
